// utils.js - Helper untuk bayar otomatis & format

function showToast(msg, type = 'success') {
    // code toast sama seperti sebelumnya
}

function formatRupiah(angka) {
    // code format sama seperti sebelumnya
}

function addToCart(productId, productData, selectedOption, inputValues) {
    // code addToCart sama seperti sebelumnya
}

function removeFromCart(key) {
    // code remove sama seperti sebelumnya
}
